import { Colors } from '@/constants/Colors'
import { View, Text } from 'react-native'

const CalculatorApp = () => {
  return (
    <View>
      <Text style={{ fontSize: 25, fontFamily: 'SpaceMono', color: Colors.textPrimary }}>
        ESTA ES LA APP
      </Text>
    </View>
  )
}
export default CalculatorApp